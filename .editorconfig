# https://EditorConfig.org

root = true

[*]
end_of_line = lf
indent_size = 2
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true

# don't trim markdown trailing whitespace to preserve new lines
[*.md]
trim_trailing_whitespace = false
